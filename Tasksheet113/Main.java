package Tasksheet113;

public class Main {

    public static void main(String[] args) {
      byte three = 3;
      byte one = 1;
      byte zero = 0;
      float version = 2.0f;
      boolean condition = true; 
    
      String output = "H" + three + one + one + zero + " " + "w" + zero + "r" + one + "d" + " " + version + " " + condition;
      System.out.println(output);

    //    byte zero = 0;
    //      String output = "W" + zero + "w";
    //      System.out.println(output);
    }
  }